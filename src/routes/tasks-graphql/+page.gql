query Tasks @cache(policy: CacheAndNetwork) {
  tasks(limit: 5, orderBy: { id: DESC }) {
    totalCount
    items {
      id
      title
      completed
    }
    # id
    # title
    # dueDate
    # completed
    # thePriority
    # category {
    #   name
    # }
  }
}
